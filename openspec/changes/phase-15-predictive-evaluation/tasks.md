# Phase 15 Predictive Evaluation — Implementation Tasks

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | Phase 15 audit remediation: 2,600–4,000 authored additions plus deletions across production and deterministic synthetic tests; planning/review metadata is additional |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | No PR while native receipt issue #1793 is unresolved. Apply WU-R01 → WU-R16 serially; each unit has an autonomous start, finish, verification, rollback, Engram evidence topic, and hard ≤400 authored changed-line boundary. After authority is restored, decide delivery boundaries without recombining units. |
| Delivery strategy | blocked/no-pr due native receipt issue #1793 |
| Chain strategy | pending |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

Task state: 63 total; 59 complete tasks; 4 open tasks; 59 implementation-owned and 4 parent-owned. Next dependency-ready task: T-15-R17 / WU-R17. T-15-15 remains unchecked after its failed audit; T-15-16 and T-15-17 remain blocked.

Implementation may proceed locally with synthetic fixtures because issue #1793 is an administrative delivery blocker, not a scientific apply blocker. It still prohibits branches, commits, PRs, archive, push, release, and publication. No `size:exception` is authorized. Before every remediation unit, record a changed-line forecast; stop and split again before any production edit if forecast or actual authored additions plus deletions would exceed 400. Real evaluation remains closed by export authorization, D-14-001, D-14-002, and protocol-approval gates.

## Scope Guard

The owned production surface is limited to `src/pada3dacb/evaluation/`, `scripts/evaluate.py`, `configs/evaluation/predictive.yaml`, and the declared `pyproject.toml` dependency update. No task owns or may modify training, model, loss, adaptation, checkpoint, split, preprocessing, experiment-producing configuration, immutable export, concept-evaluation, manuscript, or Phase 16 paths. A later independently reviewed defect may expand scope only through an explicit amendment to `specs/phase_15_predictive_evaluation/agent_plan.yaml` before the new path is edited.

## Forecast Implementation Work Units

Each work unit has a hard pre-slice measurement gate and a `target_changed_lines: <=400`; this is a review target, never a guarantee. WU-01 through WU-03 remain complete. WU-04A is the next serial start/finish/verification/rollback boundary, followed by WU-04B, WU-04C, and WU-04D. The schema prerequisite remains under conceptual `codex` action `implement-discovery-provenance-aggregation`; the later metric/matrix slices remain under `implement-metrics-and-confusion-matrices`. Repeated serial subsets of already-owned paths are not simultaneous ownership and create no active ownership collision.

| Unit | Behavior boundary | Depends on | Verification | Rollback boundary |
|---|---|---|---|---|
| WU-01 | Canonical schemas, versions, serialization, and error contracts with tests (complete) | T-15-03 PASS; `feature-branch-chain` selected; exact three-path forecast ≤370 additions plus deletions | `python -m pytest -q tests/test_evaluation_schemas.py` | Schemas and focused test only |
| WU-01A | Missing design-required frozen canonical contracts; forecast ≤400 | WU-01 | `python -m pytest -q tests/test_evaluation_schemas.py` | Contract additions and focused schema tests only |
| WU-02A | Provenance and privacy core; forecast ≤370 | WU-01A | `python -m pytest -q tests/test_evaluation_provenance.py` | Provenance module, fixture subset, and provenance tests only |
| WU-02B | Configured discovery and shared-method adapter; forecast ≤380 | WU-02A | `python -m pytest -q tests/test_evaluation_discovery.py tests/test_evaluation_provenance.py` | Discovery/shared-adapter changes and focused fixture/tests only |
| WU-02C | Baseline-combined adapter and seven-method matrix; forecast ≤345 | WU-02B | `python -m pytest -q tests/test_evaluation_discovery.py tests/test_evaluation_provenance.py` | Baseline adapter/matrix changes and focused fixture/tests only |
| WU-02D | Frozen `SubjectPrediction` final-row contract and canonical serialization; forecast ≤200 | WU-02C | `python -m pytest -q tests/test_evaluation_schemas.py` | SubjectPrediction contract and focused schema tests only |
| WU-03 | Source OOF and target fold-then-seed aggregation with tests | WU-02D | `python -m pytest -q tests/test_evaluation_aggregation.py` | Aggregation and focused test only |
| WU-04A | Frozen `MetricSet`, `PerClassMetric`, and `ConfusionResult` schema prerequisites; forecast 130–210, hard ≤400 | WU-03 | `python -m pytest -q tests/test_evaluation_schemas.py` | Metric/confusion contracts and focused schema tests only |
| WU-04B | Count-derived per-class and aggregate metrics; forecast 330–395, hard ≤400 | WU-04A | `python -m pytest -q tests/test_evaluation_metrics.py` | Count-derived metric implementation and focused tests only |
| WU-04C | Probability/ranking metrics; forecast 260–350, hard ≤400 | WU-04B | `python -m pytest -q tests/test_evaluation_metrics.py` | Probability/ranking metric additions and focused tests only, preserving WU-04B |
| WU-04D | Fixed count and nullable normalized confusion matrices; forecast 180–260, hard ≤400 | WU-04C | `python -m pytest -q tests/test_evaluation_confusion_matrices.py` | Confusion implementation and focused tests only |
| WU-05A | Frozen `BootstrapInterval`, `PairedDifference`, `McNemarResult`, and `HolmRow` schema prerequisites; forecast 180–280, hard ≤400 | WU-04D | `python -m pytest -q tests/test_evaluation_schemas.py` | Statistical result contracts and focused schema tests only |
| WU-05B | Deterministic stratified bootstrap; forecast 260–380, hard ≤400 | WU-05A | `python -m pytest -q tests/test_evaluation_bootstrap.py` | Bootstrap module and focused test only |
| WU-05C | Exact McNemar and paired stratified bootstrap; forecast 320–400, hard ≤400 | WU-05B | `python -m pytest -q tests/test_evaluation_paired_statistics.py` | Paired-statistics module and focused test only |
| WU-05D | Direct six-hypothesis Holm correction; forecast 180–300, hard ≤400 | WU-05C | `python -m pytest -q tests/test_evaluation_multiple_testing.py` | Multiple-testing module and focused test only |
| WU-06A | Independent metric and confusion references; forecast 280–390, hard ≤400 | WU-05D | `python -m pytest -q tests/test_evaluation_metrics_reference.py` | Metric/confusion reference test only |
| WU-06B | Independent bootstrap, McNemar, paired, and statsmodels Holm references; forecast 300–400, hard ≤400 | WU-06A | `python -m pytest -q tests/test_evaluation_statistics_reference.py` | Statistical reference test only |
| WU-06C | Independent statistical/availability edge cases; forecast 180–300, hard ≤400 | WU-06B | `python -m pytest -q tests/test_evaluation_statistics_edge_cases.py` | Statistical edge-case test only |
| WU-07 | Atomic table/figure projections | WU-06C | `python -m pytest -q tests/test_evaluation_tables.py` | Tables and focused test only |
| WU-08A | Frozen computational/plan/bundle schema prerequisites; forecast 180–300, hard ≤400 | WU-07 | `python -m pytest -q tests/test_evaluation_schemas.py` | Report schema contracts only |
| WU-08B | Report modes, gates, and computational extraction; forecast 300–400, hard ≤400 | WU-08A | `python -m pytest -q tests/test_evaluation_report.py` | Report state/computational slice only |
| WU-08C | Exact output plan, whole-run commit, and guarded overwrite; forecast 300–400, hard ≤400 | WU-08B | `python -m pytest -q tests/test_evaluation_output_schema.py` | Output/commit slice only |
| WU-08D | Completed read-only reuse verification; forecast 220–350, hard ≤400 | WU-08C | `python -m pytest -q tests/test_evaluation_reuse.py` | Reuse slice only |
| WU-08E1 | Resolved predictive config and direct dependencies; forecast 100–180, hard ≤400 | WU-08D | `python -m pytest -q tests/test_evaluation_cli.py -k config` | Config/dependency entries only |
| WU-08E2 | Safe CLI parser, selectors, modes, roots, and exit mapping; forecast 300–400, hard ≤400 | WU-08E1 | `python -m pytest -q tests/test_evaluation_cli.py -k 'not default_dispatch'` | Parser and focused tests only |
| WU-08E3 | Default read-only discovery/validation dispatch and real/reuse fail-closed routing; forecast 220–360, hard ≤400 | WU-08E2 | `python -m pytest -q tests/test_evaluation_cli.py tests/test_evaluation_report.py` | Default dispatcher and focused tests only |
| WU-09A | Seven-method/two-direction fixtures, discovery, provenance, aggregation; forecast 330–395, hard ≤400 | WU-08E3 | `python -m pytest -q tests/test_evaluation_integration.py` | Integration fixture + integration test only |
| WU-09B | Statistical integration/regression coverage; forecast 150–220, hard ≤400 | WU-09A | Command in T-15-13B | Regression test only |
| WU-09C | Output, atomicity, overwrite, privacy, and scope boundaries; forecast 140–210, hard ≤400 | WU-09B | Command in T-15-13C | Boundary test only |
| WU-09D | CLI modes, synthetic executor, completed reuse, and real gate; forecast 140–210, hard ≤400 | WU-09C | Command in T-15-13D | Modes test only |
| WU-10 | Documentation after focused and full validation evidence | WU-09D | Commands in T-15-14 | Three documentation files only |
| WU-11 | Independent final audit then final rerun/attestation | WU-10 | Commands in T-15-15/T-15-16 | Final audit file only; final validation owns no repository path |

## Implementation Work

- [x] **T-15-01 — Confirm Phase 14 closure and evaluation boundary.** Dependencies: none. Owner: `opencode` (`phase14-closure-and-evaluation-audit`). Owned paths: `AGENTS.md`, `specs/phase_15_predictive_evaluation/decisions.md`, `specs/phase_15_predictive_evaluation/phase14_closure_audit.md`. RED evidence: record any conflicting repository instruction, missing Phase 14 PASS evidence, non-placeholder Phase 15 evaluator, or training/experiment path proposed for ownership. GREEN evidence: bind repository instructions, both Phase 14 PASS records, the seven approved methods, the fail-closed placeholders, Phase 15 decisions, and issue #1793 as delivery-only. TRIANGULATE evidence: cross-check OpenSpec proposal/design, decisions, and Engram closure references. REFACTOR evidence: preserve existing instructions/decisions while keeping the audit to immutable evidence, scope exclusions, and named gates. Focused command: `python -m pytest -q tests/test_all_methods_regression_phase14.py`. Runtime harness: N/A — this is a read-only closure audit and must not run evaluation. Rollback: revert only T-15-01 changes in `AGENTS.md` and `decisions.md`, and delete only `phase14_closure_audit.md`. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-01`. <!-- sdd-owner: implementation -->

- [x] **T-15-02 — Freeze the complete repository-facing statistical specification.** Dependencies: T-15-01. Owner: `claude-code` (`statistical-protocol-specification`; runtime fallback must preserve this conceptual owner). Owned paths: `specs/phase_15_predictive_evaluation/requirements.md`, `specs/phase_15_predictive_evaluation/design.md`, `specs/phase_15_predictive_evaluation/tasks.md`, `specs/phase_15_predictive_evaluation/acceptance.md`, `specs/phase_15_predictive_evaluation/statistical_protocol.md`, `specs/phase_15_predictive_evaluation/output_schema.md`. RED evidence: enumerate any contradiction among requirements, design, executable tasks, AC-15-001..048, protocol v2, and output schema v2. GREEN evidence: resolve contradictions without production edits and retain subject-level aggregation, predeclared checkpoints, exact metric formulas, paired inference, six-row Holm families, undefined handling, and exact artifacts. TRIANGULATE evidence: verify every normative formula, task gate, and filename against at least two authoritative planning artifacts. REFACTOR evidence: remove ambiguity while preserving all six contracts and protocol/schema identities. Focused command: `python -c "from pathlib import Path; p=Path('specs/phase_15_predictive_evaluation'); required=['requirements.md','design.md','tasks.md','acceptance.md','statistical_protocol.md','output_schema.md']; assert all((p/x).is_file() for x in required); assert 'phase15-statistical-v2' in (p/'statistical_protocol.md').read_text(); assert 'phase15-output-v2' in (p/'output_schema.md').read_text()"`. Runtime harness: N/A — specification-only action. Rollback: revert only these six repository-facing specification files to their pre-action bytes. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-02`. <!-- sdd-owner: implementation -->

- [x] **T-15-03 — Perform independent statistical specification review before implementation.** Dependencies: T-15-02. Owner: `kimi` (`independent-statistical-review`). Owned path: `specs/phase_15_predictive_evaluation/spec_review.md` only. RED evidence: independently list blocking defects in pseudo-replication controls, checkpoint predeclaration, paired alignment/inference, Holm family construction, undefined behavior, or exact artifact consistency. GREEN evidence: write an explicit PASS only if subject is the sole statistical unit; folds are averaged within seed and all predeclared seeds are averaged; `best_source_f1` and `last` remain separate; exact paired McNemar/bootstrap orientation and six-member Holm families are fixed; null/status/reason semantics are complete; and requirements, acceptance, protocol, output schema, and design agree exactly. TRIANGULATE evidence: check hand-worked examples for aggregation, undefined metrics, paired orientation, and Holm ties/unavailable slots. REFACTOR evidence: make findings independently reproducible without editing reviewed artifacts. Focused command: `python -c "from pathlib import Path; p=Path('specs/phase_15_predictive_evaluation/spec_review.md'); assert p.exists() and ('PASS' in p.read_text() or 'BLOCK' in p.read_text())"`. Runtime harness: N/A — independent read-only specification review. Rollback: delete only `spec_review.md`; any protocol correction requires T-15-02 rerun and a fresh independent review. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-03`. <!-- sdd-owner: implementation -->

- [x] **T-15-04 — Implement canonical schemas and public package boundary.** Dependencies: T-15-03 PASS and the selected `feature-branch-chain` strategy. WU-01 is the first synthetic apply slice and owns exactly `src/pada3dacb/evaluation/__init__.py` (≤20 forecast additions plus deletions), `src/pada3dacb/evaluation/schemas.py` (≤170), and `tests/test_evaluation_schemas.py` (≤180), with a hard total forecast ceiling of ≤370 and 30 lines reserved under 400. Stop and split before continuing if forecast or actual authored changes would exceed 400; never consume the reserve silently. Receipt #1793 prohibits creating branches, commits, or PRs. Owner: `codex` (`implement-discovery-provenance-aggregation`). RED evidence: tests fail for closed enums, frozen dataclasses, value/status/reason invariants, canonical serialization/identity, fixed classes, sanitized failures, and absence of I/O/training imports. GREEN evidence: minimum pure implementation passes. TRIANGULATE evidence: add invalid enum, non-finite available value, unavailable-without-reason, ordering, and serialization stability cases. REFACTOR evidence: centralize only pure shared contracts without adding modules. Focused command: `python -m pytest -q tests/test_evaluation_schemas.py`. Runtime harness: N/A — pure in-memory contracts. Rollback: restore the namespace placeholder and remove `schemas.py` plus its focused test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-04`. <!-- sdd-owner: implementation -->

- [x] **T-15-04A — Add the missing frozen normalization contracts before discovery work.** Dependencies: T-15-04. WU-01A owns only the already-owned `src/pada3dacb/evaluation/schemas.py` and `tests/test_evaluation_schemas.py`; forecast additions plus deletions ≤400. Owner: `codex` (`implement-discovery-provenance-aggregation`). RED evidence: focused tests fail for absent `InputFile`, generic non-identity `ProvenanceValue`, `ProvenanceRecord`, `CanonicalPrediction`, and `NormalizedBatch`, including frozen mutation, canonical validation/serialization, and forbidden I/O/training imports. GREEN evidence: add the minimum immutable canonical contracts; `ProvenanceValue` cannot carry or derive identity. TRIANGULATE evidence: cover equality, deterministic serialization, immutable nested values, and rejection of noncanonical inputs. REFACTOR evidence: keep contracts pure and import-safe without I/O or training dependencies. Focused command: `python -m pytest -q tests/test_evaluation_schemas.py`. Runtime harness: N/A — pure in-memory contracts. Pre-slice gate: measure before implementation and stop/split if forecast or actual authored changes would exceed 400; no size exception. Rollback: revert only these contract additions and focused tests, preserving completed T-15-04 behavior. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-04A`. <!-- sdd-owner: implementation -->

- [x] **T-15-05A — Implement provenance and privacy core.** Dependencies: T-15-04A. WU-02A forecast additions plus deletions ≤370. Owner: `codex` (`implement-discovery-provenance-aggregation`). Sequential path subset: `src/pada3dacb/evaluation/provenance.py`, `tests/phase15_discovery_fixtures.py`, `tests/test_evaluation_provenance.py`. RED evidence: failing tests cover streaming exact-byte SHA-256, provenance equality/conflicts, probability/duplicate/label defects, supplied-only stable hashes, and raw-ID byte leakage. GREEN evidence: approved derivation rules are explicit/injected and default to no derivation; supplied `subject_hash` is used unchanged. Baseline `subject_id` may be held only transiently for verification, is then ignored/discarded, and never enters canonical objects, errors, logs, snapshots, or outputs. TRIANGULATE evidence: missing, explicit-derived, conflicting, malformed probability, duplicate, and privacy cases. REFACTOR evidence: retain one hydration/issue taxonomy with no training imports. Focused command: `python -m pytest -q tests/test_evaluation_provenance.py`. Runtime harness: N/A — focused synthetic fixtures. Pre-slice gate: measure first and stop/split if forecast or actual exceeds 400; no size exception. Rollback: revert only this provenance/privacy slice and its fixture/test changes. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-05A`. <!-- sdd-owner: implementation -->

- [x] **T-15-05B — Implement configured discovery and the shared-method adapter.** Dependencies: T-15-05A. WU-02B forecast additions plus deletions ≤380. Owner: `codex` (`implement-discovery-provenance-aggregation`). Sequential path subset: `src/pada3dacb/evaluation/discovery.py`, `src/pada3dacb/evaluation/provenance.py` only if necessary within a 15-line forecast, `tests/phase15_discovery_fixtures.py`, `tests/test_evaluation_discovery.py`. RED evidence: failing tests cover configured-root confinement, symlink/path escape, deterministic configured ordering, shared-method hydration, explicit derivation injection, unknown methods, and incomplete/conflicting candidates. GREEN evidence: resolve only configured paths and implement the read-only shared-method adapter; ambient recursive discovery and training-registry imports are forbidden. TRIANGULATE evidence: passing shared-family methods plus unsafe path, missing rule, approved rule, conflict, and no-ambient-discovery cases. REFACTOR evidence: retain an explicit registry and shared hydration without broadening provenance. Focused command: `python -m pytest -q tests/test_evaluation_discovery.py tests/test_evaluation_provenance.py`. Runtime harness: N/A until T-15-12 wires the CLI. Pre-slice gate: measure first, including optional provenance delta, and stop/split if forecast or actual exceeds 400; no size exception. Rollback: revert only this configured-discovery/shared-adapter slice and focused fixture/tests, preserving WU-02A. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-05B`. <!-- sdd-owner: implementation -->

- [x] **T-15-05C — Implement the baseline-combined adapter and seven-method matrix.** Dependencies: T-15-05B. WU-02C forecast additions plus deletions ≤345. Owner: `codex` (`implement-discovery-provenance-aggregation`). Sequential path subset: `src/pada3dacb/evaluation/discovery.py`, `tests/phase15_discovery_fixtures.py`, `tests/test_evaluation_discovery.py`, `tests/test_evaluation_provenance.py`. RED evidence: failing tests cover baseline partitioning, all seven methods across both schema families, incomplete cells, privacy, and Source-Only target-membership proof. GREEN evidence: never equate Source-Only `target_split_assignment_hash` with required `target_evaluation_assignment_hash`; missing exact approved proof excludes with `target_evaluation_membership_unprovable`. Baseline `subject_id` remains transiently ignored/discarded and supplied `subject_hash` remains unchanged. TRIANGULATE evidence: one passing fixture per method plus exact-hash mismatch, absent evaluation-assignment proof, incomplete cell, conflict, and byte-leak assertions. REFACTOR evidence: both adapters return the same `NormalizedBatch`; downstream behavior does not branch by family. Focused command: `python -m pytest -q tests/test_evaluation_discovery.py tests/test_evaluation_provenance.py`. Runtime harness after T-15-12: `python scripts/evaluate.py --config configs/evaluation/predictive.yaml --runs-root tests/fixtures/phase15-nonexistent --both-directions --all-methods --dry-run`; until then record `N/A — CLI not yet implemented`. Pre-slice gate: measure first and stop/split if forecast or actual exceeds 400; no size exception. Rollback: revert only this baseline-adapter/matrix slice and focused fixture/tests, preserving WU-02A/B. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-05C`. <!-- sdd-owner: implementation -->

- [x] **T-15-05D — Add the frozen final subject-prediction contract required by aggregation.** Dependencies: T-15-05C. WU-02D owns only the already-owned `src/pada3dacb/evaluation/schemas.py` and `tests/test_evaluation_schemas.py`; forecast additions plus deletions ≤200, with a hard stop and resplit if forecast or actual authored changes would exceed 400. Owner: `codex` (`implement-discovery-provenance-aggregation`). RED evidence: focused tests fail because frozen `SubjectPrediction` is absent or permits noncanonical field ordering, mutable values, fake seed/fold identity, or unstable serialization. GREEN evidence: add the minimum pure frozen contract representing exactly one final row per subject/method/direction/checkpoint policy with ordered probabilities, predicted label, true label, `fold_count`, `seed_count`, ordered source hashes, and status/reason when applicable; counts summarize aggregation completeness and MUST NOT invent seed or fold identity. TRIANGULATE evidence: cover canonical ordering, fixed serialized field/value order, frozen mutation rejection, deterministic equality/serialization, applicable status/reason combinations, and rejection of noncanonical probabilities, labels, counts, or hashes. REFACTOR evidence: keep serialization pure and canonical with no I/O, aggregation logic, or scientific-rule changes. Focused command: `python -m pytest -q tests/test_evaluation_schemas.py`. Runtime harness: N/A — pure in-memory contract. Pre-slice gate: measure first and stop/split above 400; no size exception. Rollback: revert only `SubjectPrediction` and its focused tests, preserving all completed schema/discovery/provenance behavior. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-05D`. <!-- sdd-owner: implementation -->

- [x] **T-15-06 — Implement source OOF and target fold-then-seed aggregation.** Dependencies: T-15-05D. Owner: `codex` (`implement-discovery-provenance-aggregation`). Owned paths: `src/pada3dacb/evaluation/aggregation.py`, `tests/test_evaluation_aggregation.py`. RED evidence: hand-calculated tests fail for source OOF missing/duplicate membership, target fold means before complete seed means, partial folds/seeds, true-label conflicts, fixed-order argmax ties, policy/direction isolation, and exactly one final row. GREEN evidence: minimal float64 in-memory aggregation passes. TRIANGULATE evidence: vary fold/seed counts and reverse direction while proving no pseudo-replication or pooling. REFACTOR evidence: isolate completeness validation from deterministic aggregation. Focused command: `python -m pytest -q tests/test_evaluation_aggregation.py`. Runtime harness: N/A — pure in-memory aggregation; validate-only harness is owned by T-15-12/T-15-13. Rollback: remove only `aggregation.py` and its focused test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-06`. <!-- sdd-owner: implementation -->

- [x] **T-15-07A — Add frozen metric and confusion-result schema prerequisites.** Dependencies: T-15-06. WU-04A forecast additions plus deletions 130–210, hard ≤400. Owner: `codex` (`implement-discovery-provenance-aggregation`) to preserve existing ownership of `src/pada3dacb/evaluation/schemas.py` and `tests/test_evaluation_schemas.py`; no path ownership transfers. RED evidence: focused tests fail because frozen `MetricSet`, `PerClassMetric`, and `ConfusionResult` contracts are absent or permit mutable/noncanonical class order, incomplete metric rows, contradictory `value/status/reason`, malformed 3x3 matrices, or unstable serialization. GREEN evidence: add minimum pure frozen contracts mirroring protocol-v2 fields with fixed `(CN,MCI,AD)=(0,1,2)` order; every available metric has finite non-null value and null reason, every unavailable metric has null value and a stable allowed reason; confusion counts are integer 3x3 and normalized cells preserve nullable status/reason. TRIANGULATE evidence: cover equality, frozen mutation, canonical ordering/serialization, incomplete rows, invalid finite/status combinations, and malformed matrix dimensions. REFACTOR evidence: reuse `MetricValue` invariants without metric computation, I/O, or target selection. Focused command: `python -m pytest -q tests/test_evaluation_schemas.py`. Runtime harness: N/A — pure in-memory contracts. Pre-slice gate: measure first and stop/resplit above 400; no size exception. Rollback: revert only these three contracts and focused schema tests, preserving completed schema/aggregation behavior. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-07A`. <!-- sdd-owner: implementation -->

- [x] **T-15-07B — Implement count-derived metrics.** Dependencies: T-15-07A. WU-04B forecast additions plus deletions 330–395, hard ≤400. Owner: `codex` (`implement-metrics-and-confusion-matrices`). Sequential path subset: `src/pada3dacb/evaluation/metrics.py`, `tests/test_evaluation_metrics.py`. RED evidence: failing tests cover per-class support, precision, recall/sensitivity, specificity, and F1 plus aggregate accuracy, balanced accuracy, macro-F1, weighted F1, macro precision, macro recall, multiclass MCC, and Cohen's kappa using float64 and fixed labels. GREEN evidence: implement exact protocol-v2 formulas and denominator guards without warning coercion, inferred labels, or target-derived selection. TRIANGULATE evidence: non-degenerate, empty, missing-class, missing-negative, no-predicted-positive, zero-F1, zero-MCC, and zero-kappa cases. For aggregate metrics dependent on per-class values, scan fixed class order `CN,MCI,AD` and propagate the first unavailable constituent's reason deterministically; unavailable values remain null, never zero. REFACTOR evidence: centralize only count-derived guards and canonical result construction. Focused command: `python -m pytest -q tests/test_evaluation_metrics.py`. Runtime harness: N/A — pure statistical core. Pre-slice gate: measure first and stop/resplit above 400; no size exception. Rollback: remove only count-derived `metrics.py` behavior and its focused tests. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-07B`. <!-- sdd-owner: implementation -->

- [x] **T-15-07C — Implement probability and ranking metrics.** Dependencies: T-15-07B. WU-04C forecast additions plus deletions 260–350, hard ≤400. Owner: `codex` (`implement-metrics-and-confusion-matrices`). Sequential path subset: `src/pada3dacb/evaluation/metrics.py`, `tests/test_evaluation_metrics.py`. RED evidence: failing tests cover exact float64 log loss with `eps=np.finfo(np.float64).eps`, clipping to `[eps,1-eps]`, per-row renormalization, and fixed labels; unscaled multiclass Brier in `[0,2]`; per-class OVR ROC-AUC and non-interpolated average precision; and macro AUC/AP availability. GREEN evidence: implement exact protocol-v2 probability/ranking formulas using original validated probabilities except the explicitly clipped-renormalized log-loss copy; no inferred labels or target selection. TRIANGULATE evidence: ties, boundary probabilities, missing positives/negatives, invalid probabilities, and deliberately non-finite computed results. Macro AUC/AP scan `CN,MCI,AD` and propagate the first unavailable constituent's reason deterministically; any non-finite computed probability metric result is unavailable with allowed reason `non_finite_input`. REFACTOR evidence: share validation/result construction while preserving WU-04B behavior. Focused command: `python -m pytest -q tests/test_evaluation_metrics.py`. Runtime harness: N/A — pure statistical core. Pre-slice gate: measure first and stop/resplit above 400; no size exception. Rollback: revert only probability/ranking additions and their focused tests, preserving WU-04B. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-07C`. <!-- sdd-owner: implementation -->

- [x] **T-15-07D — Implement fixed confusion matrices.** Dependencies: T-15-07C. WU-04D forecast additions plus deletions 180–260, hard ≤400. Owner: `codex` (`implement-metrics-and-confusion-matrices`). Owned paths: `src/pada3dacb/evaluation/confusion_matrices.py`, `tests/test_evaluation_confusion_matrices.py`. RED evidence: failing tests cover fixed 3x3 count matrices with true rows/predicted columns in `(CN,MCI,AD)` order, supported true-row normalization, integer zero preservation, and nullable zero-support rows. GREEN evidence: derive counts and normalized rows only from the final canonical subject table for the exact method/direction/policy; each zero-support normalized row is three null unavailable cells with reason `zero_true_support`. TRIANGULATE evidence: non-degenerate, absent true class, absent predicted class, and empty-table cases with exact row sums/statuses. REFACTOR evidence: keep matrix construction pure and separate from projections, I/O, metrics, and target selection. Focused command: `python -m pytest -q tests/test_evaluation_confusion_matrices.py`. Runtime harness: N/A — pure statistical core. Pre-slice gate: measure first and stop/resplit above 400; no size exception. Rollback: remove only `confusion_matrices.py` and its focused test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-07D`. <!-- sdd-owner: implementation -->

- [x] **T-15-08A — Add frozen inferential-result schema prerequisites.** Dependencies: T-15-07D. WU-05A forecast additions plus deletions 180–280, hard ≤400. Owner: `codex` (`implement-discovery-provenance-aggregation`) to preserve existing ownership of `src/pada3dacb/evaluation/schemas.py` and `tests/test_evaluation_schemas.py`; no ownership transfer. RED evidence: focused tests fail because frozen `BootstrapInterval`, `PairedDifference`, `McNemarResult`, and `HolmRow` are absent or permit invalid available/unavailable states. GREEN evidence: add minimum protocol-v2 contracts with finite/null invariants, requested/successful/invalid count consistency, optional informational `note_code`, six-slot comparator identity, and canonical serialization. TRIANGULATE evidence: mutation, count, reason/note, p-value/range, comparator, and serialization cases. REFACTOR evidence: reuse `ValueStatus` semantics and keep contracts free of computation, I/O, and target selection. Focused command: `python -m pytest -q tests/test_evaluation_schemas.py`. Runtime harness: N/A. Rollback: remove only inferential contracts and focused tests. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-08A`. <!-- sdd-owner: implementation -->

- [x] **T-15-08B — Implement deterministic stratified bootstrap.** Dependencies: T-15-08A. WU-05B forecast additions plus deletions 260–380, hard ≤400. Owner: `codex` (`implement-bootstrap-and-statistics`). Owned paths: `src/pada3dacb/evaluation/bootstrap.py`, `tests/test_evaluation_bootstrap.py`. RED evidence: failing tests cover PCG64 deterministic true-class fixed-order sampling, one draw/no redraw, positive replicate validation, per-metric successful/invalid counts, linear percentile intervals, and the exact `ceil(0.95B)` threshold. GREEN evidence: minimal pure bootstrap passes. TRIANGULATE evidence: hand-worked and deterministic small-B cases plus B=10000 repeatability where affordable. REFACTOR evidence: keep RNG and draw order explicit, metric-callable based, bounded, and I/O-free. Focused command: `python -m pytest -q tests/test_evaluation_bootstrap.py`. Runtime harness: N/A. Rollback: remove only bootstrap module and focused test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-08B`. <!-- sdd-owner: implementation -->

- [x] **T-15-08C — Implement exact McNemar and paired stratified bootstrap.** Dependencies: T-15-08B. WU-05C forecast additions plus deletions 320–400, hard ≤400. Owner: `codex` (`implement-bootstrap-and-statistics`). Owned paths: `src/pada3dacb/evaluation/paired_statistics.py`, `tests/test_evaluation_paired_statistics.py`. RED evidence: failing tests cover strict ordered subject/label alignment, exact two-sided SciPy McNemar including zero discordance, shared paired indices, `prototype_pseudo-comparator` orientation, no redraw, exact success threshold/counts, raw-difference linear intervals, and centered plus-one p-values. GREEN evidence: minimal pure paired implementation passes. TRIANGULATE evidence: hand-worked discordance, incompatible subjects, unavailable observed metrics, small-B deterministic draws, and repeatability. REFACTOR evidence: share only pure stratified-index mechanics and keep pairing explicit and I/O-free. Focused command: `python -m pytest -q tests/test_evaluation_paired_statistics.py`. Runtime harness: N/A. Rollback: remove only paired-statistics module and focused test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-08C`. <!-- sdd-owner: implementation -->

- [x] **T-15-08D — Implement direct six-hypothesis Holm correction.** Dependencies: T-15-08C. WU-05D forecast additions plus deletions 180–300, hard ≤400. Owner: `codex` (`implement-bootstrap-and-statistics`). Owned paths: `src/pada3dacb/evaluation/multiple_testing.py`, `tests/test_evaluation_multiple_testing.py`. RED evidence: failing tests cover exactly six fixed comparator rows, canonical comparator tie order, family size retained across unavailable slots, monotone adjusted values, raw/adjusted reporting, and no all-pairs expansion. GREEN evidence: minimal direct protocol-v2 Holm implementation passes. TRIANGULATE evidence: hand-worked ties, unavailable slots, boundary p-values, and canonical output order. REFACTOR evidence: keep family construction and sorting explicit, deterministic, dependency-free, and I/O-free. Focused command: `python -m pytest -q tests/test_evaluation_multiple_testing.py`. Runtime harness: N/A. Rollback: remove only multiple-testing module and focused test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-08D`. <!-- sdd-owner: implementation -->

- [x] **T-15-09A — Independently verify metrics and confusion matrices.** Dependencies: T-15-08D. WU-06A forecast additions plus deletions 280–390, hard ≤400. Owner: `gemini-cli` (`independent-mathematical-statistical-verification`; runtime fallback preserves conceptual ownership). Owned path: `tests/test_evaluation_metrics_reference.py`. RED evidence: independent fixed-class references fail defects in all twelve aggregate metrics, seven distinct per-class quantities/eight rows, clipping, unscaled Brier, OVR AUC/AP availability, and confusion normalization. GREEN evidence: production passes unchanged without weakening references. TRIANGULATE evidence: direct formulas plus scikit-learn and hand-calculated matrices, including float64 boundaries and absent classes. REFACTOR evidence: keep reference calculations structurally independent of production helpers. Focused command: `python -m pytest -q tests/test_evaluation_metrics_reference.py`. Runtime harness: N/A. Rollback: remove only the metric reference test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-09A`. <!-- sdd-owner: implementation -->

- [x] **T-15-09B — Independently verify bootstrap, paired inference, and Holm.** Dependencies: T-15-09A. WU-06B forecast additions plus deletions 300–400, hard ≤400. Owner: `gemini-cli` (`independent-mathematical-statistical-verification`). Owned path: `tests/test_evaluation_statistics_reference.py`. RED evidence: independent NumPy/SciPy/statsmodels references fail defects in PCG64 stratified draws, linear quantiles, exact counts/thresholds, exact McNemar, paired orientation/centered plus-one p-values, and six-slot Holm. GREEN evidence: production passes unchanged without weakening references. TRIANGULATE evidence: hand calculations plus NumPy, SciPy `binomtest`, and statsmodels `multipletests(method="holm")`. REFACTOR evidence: keep reference algorithms independent of production helpers. Focused command: `python -m pytest -q tests/test_evaluation_statistics_reference.py`. Runtime harness: N/A. Rollback: remove only the statistical reference test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-09B`. <!-- sdd-owner: implementation -->

- [x] **T-15-09C — Independently verify statistical and availability edge cases.** Dependencies: T-15-09B. WU-06C forecast additions plus deletions 180–300, hard ≤400. Owner: `gemini-cli` (`independent-mathematical-statistical-verification`). Owned path: `tests/test_evaluation_statistics_edge_cases.py`. RED evidence: independent acceptance cases fail missing classes, no predicted positives, degenerate denominators, zero support, invalid bootstrap thresholds, zero discordance, incompatible paired subjects/labels, unavailable paired metrics, unavailable Holm slots, and no all-pairs behavior. GREEN evidence: production passes without weakening acceptance assertions; any discovered implementation defect requires a separately owned correction before completion. TRIANGULATE evidence: exact statuses/reasons/counts/nulls across boundary cases. REFACTOR evidence: keep edge fixtures small, deterministic, CPU-only, and independent. Focused command: `python -m pytest -q tests/test_evaluation_statistics_edge_cases.py`. Runtime harness: N/A. Rollback: remove only the edge-case reference test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-09C`. <!-- sdd-owner: implementation -->

- [x] **T-15-10 — Implement atomic table and figure projections.** Dependencies: T-15-09. Owner: `opencode` (`tables-report-cli-integration`). Owned paths for this slice: `src/pada3dacb/evaluation/tables.py`, `tests/test_evaluation_tables.py`. RED evidence: failing tests cover exact CSV column/order/null encoding, canonical JSON/YAML, deterministic count/normalized PNG metadata, subject-table hash binding, same-filesystem temporary siblings, atomic replacement, and cleanup after injected failures. GREEN evidence: minimum atomic single-artifact writers and exact schema-v2 projections pass. TRIANGULATE evidence: exercise JSON/YAML/CSV/PNG and zero-support matrices. REFACTOR evidence: separate pure projections from writer seams. Focused command: `python -m pytest -q tests/test_evaluation_tables.py`. Runtime harness: N/A — output orchestration is not wired until T-15-11/T-15-12. Rollback: remove only `tables.py` and its focused test. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-10`. <!-- sdd-owner: implementation -->

- [x] **T-15-11A — Add frozen report schema prerequisites.** Dependencies: T-15-10. WU-08A forecast additions plus deletions 180–300, hard ≤400. Owner: `codex` (`implement-discovery-provenance-aggregation`) to preserve existing ownership of `src/pada3dacb/evaluation/schemas.py` and `tests/test_evaluation_schemas.py`. RED evidence: focused tests fail because `ComputationalValue`, `EvaluationPlan`, and `EvaluationBundle` are absent or permit mutable/incomplete identity, output, subject-table, or artifact contracts. GREEN evidence: minimum frozen canonical contracts pass. TRIANGULATE evidence: finite/null status, units/source hashes, exact relative paths, immutable mappings/tuples, identity consistency, and canonical serialization. REFACTOR evidence: pure contracts only, no I/O/statistics/training imports. Focused command: `python -m pytest -q tests/test_evaluation_schemas.py`. Rollback: report contracts/tests only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-11A`. <!-- sdd-owner: implementation -->

- [x] **T-15-11B — Implement report modes, gates, and computational extraction.** Dependencies: T-15-11A. WU-08B forecast additions plus deletions 300–400, hard ≤400. Owner: `opencode` (`tables-report-cli-integration`). Owned paths: `src/pada3dacb/evaluation/report.py`, `tests/test_evaluation_report.py`. RED evidence: failing tests cover dry-run/validate-only no-write boundaries, evaluate gate stop before statistics, explicit state transitions, canonical subject-table requirement, and computational available/not-recorded/conflict values with units/source hashes. GREEN evidence: minimal deterministic injected-seam orchestration passes. TRIANGULATE evidence: every mode, gate denial, available/missing/conflicting computational records, and zero writer/statistic calls where prohibited. REFACTOR evidence: explicit transitions and pure extraction separated from output commit. Focused command: `python -m pytest -q tests/test_evaluation_report.py`. Rollback: state/extraction report slice only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-11B`. <!-- sdd-owner: implementation -->

- [x] **T-15-11C — Implement exact output plan, whole-run commit, and guarded overwrite.** Dependencies: T-15-11B. WU-08C forecast additions plus deletions 300–400, hard ≤400. Owner: `opencode` (`tables-report-cli-integration`). Owned paths: `src/pada3dacb/evaluation/report.py`, `tests/test_evaluation_output_schema.py`. RED evidence: failing tests cover exact root/policy paths and required names, header-complete exclusions, staging, manifest-last completion, recognized-path collision, guarded overwrite allowlist, backup restoration, and injected commit failure cleanup. GREEN evidence: minimal whole-run commit passes using synthetic byte artifacts and injected replace seams. TRIANGULATE evidence: new output, exclusions, existing output, successful overwrite, failed overwrite restoration, and no completed manifest on failure. REFACTOR evidence: centralized relative-path allowlist and explicit manifest-last order. Focused command: `python -m pytest -q tests/test_evaluation_output_schema.py`. Rollback: output/commit additions and focused test only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-11C`. <!-- sdd-owner: implementation -->

- [x] **T-15-11D — Implement completed read-only reuse verification.** Dependencies: T-15-11C. WU-08D forecast additions plus deletions 220–350, hard ≤400. Owner: `opencode` (`tables-report-cli-integration`). Owned paths: `src/pada3dacb/evaluation/report.py`, `tests/test_evaluation_reuse.py`. RED evidence: failing tests cover missing completion manifest, evaluation/config/authorization/protocol/schema/library/input identity mismatch, required-file/hash mismatch, exact completed reuse, and proof that reuse performs no writes. GREEN evidence: deterministic read-only verifier passes. TRIANGULATE evidence: exact reuse plus each mismatch category and unknown-file tolerance only where protocol permits. REFACTOR evidence: separate expected-identity derivation from read-only file/hash verification. Focused command: `python -m pytest -q tests/test_evaluation_reuse.py`. Rollback: reuse additions and focused test only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-11D`. <!-- sdd-owner: implementation -->

- [x] **T-15-12A — Declare the resolved predictive configuration and direct dependencies.** Dependencies: T-15-11D. WU-08E1 forecast additions plus deletions 100–180, hard ≤400. Owner: `opencode` (`tables-report-cli-integration`). Sequential paths: `configs/evaluation/predictive.yaml`, `pyproject.toml`, and the config/dependency slice of `tests/test_evaluation_cli.py`. RED evidence: focused tests fail until the config explicitly declares approved methods/directions/class order, schema families/patterns, folds/seeds, source-defined policies, gates and no target-derived selectors; SciPy is direct runtime; existing NumPy/scikit-learn/PyYAML/matplotlib remain direct runtime; statsmodels is dev/test-only. GREEN evidence: minimum deterministic YAML and dependency declarations pass. TRIANGULATE evidence: parse config, inspect runtime/dev groups, and prove no raw IDs, secrets, executable logic, implicit selectors, or invented scientific defaults. REFACTOR evidence: configuration data only. Focused command: `python -m pytest -q tests/test_evaluation_cli.py -k config`. Rollback: remove predictive config and revert only Phase 15 dependency entries. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-12A`. <!-- sdd-owner: implementation -->

- [x] **T-15-12B — Integrate the safe CLI parser and fail-closed state mapping.** Dependencies: T-15-12A. WU-08E2 forecast additions plus deletions 300–400, hard ≤400. Owner: `opencode` (`tables-report-cli-integration`). Sequential paths: `scripts/evaluate.py` and the parser/state slice of `tests/test_evaluation_cli.py`. RED evidence: parser/state tests fail every required flag, selector conflict, nonpositive bootstrap count, missing evaluate seed/output root, forbidden seed outside evaluate, inspection/reuse/overwrite conflicts, sensitivity ambiguity, root overlap, parser/config exit separation, exit categories, no target-derived selectors, and static training/statistics-import boundaries. GREEN evidence: thin argparse/resolution/exit wiring passes without scientific work in the parser. TRIANGULATE evidence: single/both directions, selected/all methods, primary plus separate sensitivity, every mode, and invalid-combination matrix. REFACTOR evidence: keep science outside CLI and adapters outside training registries. Focused command: `python -m pytest -q tests/test_evaluation_cli.py -k 'not default_dispatch'`. Rollback: restore fail-closed parser and remove parser/state tests. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-12B`. <!-- sdd-owner: implementation -->

- [x] **T-15-12C — Connect the default safe read-only dispatcher.** Dependencies: T-15-12B. WU-08E3 forecast additions plus deletions 220–360, hard ≤400. Owner: `opencode` (`tables-report-cli-integration`). Sequential paths: `scripts/evaluate.py` and the default-dispatch slice of `tests/test_evaluation_cli.py`. RED evidence: default dry-run, validate-only, real-gate, incomplete, reuse, and parser-exit tests expose false success or prohibited work. GREEN evidence: config resolution plus configured discovery and validation use existing evaluation adapters/aggregation; real evaluate stops at the closed gate before loading/statistics; reuse or unavailable synthetic evaluation fails closed rather than returning success. TRIANGULATE evidence: complete/incomplete discovery, valid/invalid ensemble alignment, gate denial, missing reuse identity/root, and zero writes/metrics/training imports. REFACTOR evidence: script only coordinates existing evaluation modules and maps stable categories. Focused command: `python -m pytest -q tests/test_evaluation_cli.py tests/test_evaluation_report.py`. Runtime harness remains T-15-13-owned. Rollback: remove default dispatch additions/tests only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-12C`. <!-- sdd-owner: implementation -->

- [x] **T-15-13A — Integrate seven methods, both directions, provenance, and aggregation.** Dependencies: T-15-12C. WU-09A forecast 330–395, hard ≤400. Owner: `codex` (`complete-integration-and-regression-tests`). Paths: `tests/phase15_integration_fixtures.py`, `tests/test_evaluation_integration.py`. RED/GREEN/TRIANGULATE/REFACTOR: absent matrix harness → deterministic two-family seven-method/two-direction fixtures through production discovery/normalization/aggregation → complete/incomplete/conflicting/policy cases → consolidate fixtures without production edits. Covers AC-15-001..017. Focused command: `python -m pytest -q tests/test_evaluation_integration.py --basetemp=artifacts/pytest-tmp-phase15-integration-a`. Rollback: two integration files only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-13A`. <!-- sdd-owner: implementation -->

- [x] **T-15-13B — Integrate statistical and previous-method regressions.** Dependencies: T-15-13A. WU-09B forecast 150–220, hard ≤400. Owner: `codex` same action. Path: `tests/test_evaluation_regressions.py`. Drive canonical final tables through metrics/confusion/bootstrap/paired/Holm and bind publication rows to subject-table hashes; cover AC-15-018..035 without duplicating focused reference ownership. Focused command: `python -m pytest -q tests/test_evaluation_regressions.py tests/test_evaluation_metrics_reference.py tests/test_evaluation_statistics_reference.py tests/test_evaluation_statistics_edge_cases.py --basetemp=artifacts/pytest-tmp-phase15-integration-b`. Rollback: regression test only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-13B`. <!-- sdd-owner: implementation -->

- [x] **T-15-13C — Integrate output, atomicity, privacy, and scope boundaries.** Dependencies: T-15-13B. WU-09C forecast 140–210, hard ≤400. Owner: `codex` same action. Path: `tests/test_evaluation_boundaries.py`. Cover AC-15-036..041 and AC-15-047..048 using exact output plans/manifests/commit, input hash snapshots, overwrite restoration, computational nulls, raw-ID/training/concept/manuscript/Phase16 absence. Focused command: `python -m pytest -q tests/test_evaluation_boundaries.py tests/test_evaluation_output_schema.py tests/test_evaluation_tables.py --basetemp=artifacts/pytest-tmp-phase15-integration-c`. Rollback: boundary test only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-13C`. <!-- sdd-owner: implementation -->

- [x] **T-15-13D — Integrate CLI modes, synthetic execution, completed reuse, and real gate.** Dependencies: T-15-13C. WU-09D forecast 140–210, hard ≤400. Owner: `codex` same action. Path: `tests/test_evaluation_modes.py`. Cover AC-15-042, AC-15-043/043A, AC-15-044..046 via injected synthetic executor for successful synthetic evaluate/reuse, while separately proving default dispatch remains fail-closed without independently resolved evidence and inspection paths remain unwritten. Focused/runtime command: `python -m pytest -q tests/test_evaluation_modes.py tests/test_evaluation_cli.py tests/test_evaluation_report.py tests/test_evaluation_reuse.py --basetemp=artifacts/pytest-tmp-phase15-integration-d`. Rollback: modes test only. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-13D`. <!-- sdd-owner: implementation -->

- [x] **T-15-13E — Reconcile stale Phase 14 guards with the authorized Phase 15 boundary.** Dependencies: T-15-13D and explicit maintainer authorization after the T-15-14 pre-documentation suite exposed two stale assertions. Owner: `codex` (`complete-integration-and-regression-tests`; runtime fallback preserves conceptual ownership). Owned paths: `tests/test_all_methods_regression_phase14.py`, `tests/test_proposed_method_cli.py`. Remove only the obsolete prohibition on the authorized `evaluation/confusion_matrices.py`; preserve prohibitions on unapproved statistics, concept-intervention, ROI-stability, and experiment modules. Focused command: `python -m pytest -q tests/test_all_methods_regression_phase14.py tests/test_proposed_method_cli.py`. Rollback: restore only these two guards. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-13E`. <!-- sdd-owner: implementation -->

- [x] **T-15-14 — Validate, then document the evaluator, actual evidence, and implementation scope.** Dependencies: T-15-13E. Owner: `claude-code` (`documentation`; runtime fallback must preserve this conceptual owner). Owned paths: `docs/PREDICTIVE_EVALUATION.md`, `docs/PHASE15_REPORT.md`, `docs/IMPLEMENTATION_AUDIT.md`. RED evidence: run the focused synthetic command `python -m pytest -q tests/test_evaluation_integration.py tests/test_evaluation_modes.py tests/test_evaluation_boundaries.py tests/test_evaluation_regressions.py --basetemp=artifacts/pytest-tmp-phase15-documentation-focused` and the full sequence `python -m pip install -e .`; `python -c "import pada3dacb; print(pada3dacb.__version__)"`; `python -m pytest -q`; `python -m ruff check .`; `git diff --check`, recording failures before writing claims. GREEN evidence: after commands pass, all three documents record exact commands, exit codes, pass counts, warnings, durations, safe CLI/output/provenance behavior, statistical limitations, unresolved gates, and no real claims. TRIANGULATE evidence: commands and filenames match parser/config/output-schema tests and T-15-13 evidence. REFACTOR evidence: lead with safe usage and keep actual validation evidence in `PHASE15_REPORT.md` before final audit. Runtime harness: the focused command above, including dry-run/validate-only no-output and input-hash evidence; no real evaluation. Rollback: remove only the three Phase 15 docs; validation does not mutate production. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-14`. <!-- sdd-owner: implementation -->

## Audit Remediation Work

The failed T-15-15 findings C-01..C-05 return through the following serial units. Repeated paths are sequential subsets of their existing single conceptual action owner; they are never concurrent ownership transfers. Every implementation unit is strict TDD: save the failing RED command/output before production edits, then save GREEN, TRIANGULATE, REFACTOR, changed-line count, runtime-harness result, and rollback boundary to the named Engram topic.

- [x] **T-15-R01 / WU-R01 — Freeze approved identity-mapping and expected-population contracts.** Finding: C-02. Dependencies: failed T-15-15 audit. Owner: `codex` (`implement-discovery-provenance-aggregation`). Exact paths: `src/pada3dacb/evaluation/schemas.py`, `tests/test_evaluation_schemas.py`. Forecast: 180–300 authored additions plus deletions; hard ≤400. RED: first add failing tests for immutable approved mapping identity (mapping hash, raw-ID field name, supplied `subject_hash` field, approval state) and externally declared expected populations by direction/role with sanitized relative path, exact hash, and ordered supplied subject hashes; command `python -m pytest -q tests/test_evaluation_schemas.py`. GREEN: add only the pure frozen contracts and canonical serialization, with no I/O or local hash derivation. TRIANGULATE: reject unapproved mappings, duplicate/empty populations, unsafe paths, malformed hashes, raw-ID persistence fields, and noncanonical order. REFACTOR: retain existing schema behavior and rerun the focused command. Runtime harness: N/A — pure contracts. Rollback: revert only R01 contract/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R01`. <!-- sdd-owner: implementation -->

- [x] **T-15-R02 / WU-R02 — Declare closed identity, population, reuse-root, and gate controls in configuration.** Finding: C-02/C-04/C-01 reuse prerequisite. Dependencies: T-15-R01. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `configs/evaluation/predictive.yaml`, `tests/test_evaluation_cli.py`. Forecast: 140–260; hard ≤400. RED: add failing config tests requiring explicit approved identity-companion controls, expected-population companions, optional completed-reuse roots, and separately named real gates for authorized exports, D-14-001, D-14-002, and protocol approval; command `python -m pytest -q tests/test_evaluation_cli.py -k config`. GREEN: add data-only closed/null defaults; no real path, approval, raw identifier, secret, or claim becomes authorized. TRIANGULATE: fixture config may bind synthetic relative paths/hashes, while canonical real config remains closed and parser selectors cannot broaden inventory. REFACTOR: keep configuration deterministic and non-executable. Runtime harness: N/A — config contract only. Rollback: revert only R02 config/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R02`. <!-- sdd-owner: implementation -->

- [x] **T-15-R03 / WU-R03 — Verify approved companion mappings and block raw-ID persistence.** Finding: C-02. Dependencies: T-15-R02. Owner: `codex` (`implement-discovery-provenance-aggregation`). Exact paths: `src/pada3dacb/evaluation/provenance.py`, `tests/test_evaluation_provenance.py`. Forecast: 280–400; hard ≤400. RED: add failing tests proving an approved exact-hash raw-ID-to-supplied-`subject_hash` companion is verified transiently, unapproved/mismatched mappings emit `unapproved_identity_mapping` or `unstable_subject_identity`, and any raw-ID key/value crossing canonical/log/error/output serialization emits `raw_identifier_persistence_attempt`; command `python -m pytest -q tests/test_evaluation_provenance.py`. GREEN: implement minimum mapping verification and recursive persistence guard without generating, transforming, salting, or deriving hashes. TRIANGULATE: direct supplied hash, approved companion, unapproved companion, hash mismatch, duplicate mapping, cross-file mismatch, and byte-leak cases. REFACTOR: discard raw IDs immediately and preserve sanitized issue details. Runtime harness: N/A — synthetic in-memory/file fixtures. Rollback: revert only R03 provenance/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R03`. <!-- sdd-owner: implementation -->

- [x] **T-15-R04 / WU-R04 — Wire identity companions, expected populations, and Source-Only membership into adapters.** Finding: C-02. Dependencies: T-15-R03. Owner: `codex` (`implement-discovery-provenance-aggregation`). Exact paths: `src/pada3dacb/evaluation/discovery.py`, `tests/phase15_discovery_fixtures.py`, `tests/test_evaluation_discovery.py`. Forecast: 300–400; hard ≤400. RED: add failing adapter tests for configured companion hash approval, externally declared source/target population loading, and Source-Only lacking exact target-evaluation assignment proof producing the exact visible `target_evaluation_membership_unprovable` issue rather than `missing_required_field`; command `python -m pytest -q tests/test_evaluation_discovery.py tests/test_evaluation_provenance.py`. GREEN: normalize both families through the approved mapping/population contracts and retain exact candidate issues for later status/inclusion reports. TRIANGULATE: all seven methods, both roles, Source-Only proof present/absent/conflicting, missing expected subject, extra observed subject, and raw-ID leak rejection. REFACTOR: keep downstream `NormalizedBatch` family-neutral and read-only. Runtime harness: focused synthetic adapters only. Rollback: revert only R04 discovery/fixture/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R04`. <!-- sdd-owner: implementation -->

- [x] **T-15-R05 / WU-R05 — Enforce externally declared population completeness in default validation.** Finding: C-02. Dependencies: T-15-R04. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `scripts/evaluate.py`, `tests/test_evaluation_cli.py`. Forecast: 180–300; hard ≤400. RED: add a failing default validate-only case where one configured subject is absent from every fold/seed and observed rows otherwise look complete; command `python -m pytest -q tests/test_evaluation_cli.py -k 'default_dispatch and (population or validate_only)'`. GREEN: pass independently loaded expected subject sets into source OOF and target aggregation instead of reconstructing them from observed rows; preserve read-only inspection. TRIANGULATE: exact population, globally missing subject, unexpected subject, direction/role mismatch, and missing population declaration. REFACTOR: isolate expected-population resolution from aggregation. Runtime harness: the focused CLI command with a non-existent output root and pre/post input hashes. Rollback: revert only R05 script/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R05`. <!-- sdd-owner: implementation -->

- [x] **T-15-R06 / WU-R06 — Align completion manifest and optional artifact index exactly with schema v2.** Finding: C-03. Dependencies: T-15-R05. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `src/pada3dacb/evaluation/report.py`, `tests/test_evaluation_output_schema.py`, `tests/test_evaluation_reuse.py`. Forecast: 260–380; hard ≤400. RED: add failing tests for every required manifest field—fixed classes, bootstrap count/seed/CI policy, configuration/authorization hashes, four gate states, created/completed UTC timestamps, ordered input/output hashes, libraries, and overwrite/reuse disposition—and for optional self-excluding `artifact_index.json`; assert `evaluation_index.json` is rejected; command `python -m pytest -q tests/test_evaluation_output_schema.py tests/test_evaluation_reuse.py`. GREEN: emit/verify only schema-v2 names and exact fields. TRIANGULATE: no index, valid artifact index, index hash mismatch, legacy alias, missing manifest field, overwrite completion, and reused disposition. REFACTOR: centralize one required-manifest/index contract. Runtime harness: synthetic temporary output tree only. Rollback: revert only R06 report/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R06`. <!-- sdd-owner: implementation -->

- [x] **T-15-R07 / WU-R07 — Report every unresolved real-evaluation gate before statistics.** Finding: C-04. Dependencies: T-15-R06. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `scripts/evaluate.py`, `tests/test_evaluation_cli.py`. Forecast: 100–220; hard ≤400. RED: add a failing default real request that spies on normalization/statistics/output and asserts one sanitized report contains all unresolved tokens `authorized_exports`, `D-14-001`, `D-14-002`, and `protocol_approval`; command `python -m pytest -q tests/test_evaluation_cli.py -k real_evaluation`. GREEN: collect all gate failures before raising `AuthorizationGateError`; do not short-circuit on `authorized`. TRIANGULATE: each individual gate, all gates, malformed gate state, and all-authorized synthetic seam while real statistics remain otherwise unauthorized in repository config. REFACTOR: keep gate evaluation pure and pre-statistical. Runtime harness: default CLI against synthetic files with `analysis_mode: real`, asserting no output root. Rollback: revert only R07 script/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R07`. <!-- sdd-owner: implementation -->

- [x] **T-15-R08 / WU-R08 — Project canonical root, status, provenance, and subject-table artifacts.** Finding: C-01/C-03. Dependencies: T-15-R07. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `src/pada3dacb/evaluation/tables.py`, `tests/test_evaluation_tables.py`. Forecast: 280–400; hard ≤400. RED: add failing exact-column/order/null/privacy tests for resolved config, provenance report, method status, inclusion report, computational summary, sanitized log, and `subject_predictions/<method>.csv`; command `python -m pytest -q tests/test_evaluation_tables.py`. GREEN: add pure byte projectors bound to evaluation identity/input hashes with no raw identifiers. TRIANGULATE: included, excluded, incomplete, unavailable computational value, both directions/policies, and byte-leak cases. REFACTOR: reuse canonical JSON/YAML/CSV primitives and keep I/O outside projections. Runtime harness: N/A — pure projections. Rollback: revert only R08 table/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R08`. <!-- sdd-owner: implementation -->

- [x] **T-15-R09 / WU-R09 — Project metric, confusion, and confidence-interval artifacts.** Finding: C-01. Dependencies: T-15-R08. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `src/pada3dacb/evaluation/tables.py`, `tests/test_evaluation_tables.py`. Forecast: 300–400; hard ≤400. RED: add failing exact-schema projections for twelve aggregate metrics, 24 per-class rows, all bootstrap counts/statuses, four confusion files per included method, and publication table derivation from exact subject-table bytes; command `python -m pytest -q tests/test_evaluation_tables.py`. GREEN: implement minimum deterministic projections/PNGs. TRIANGULATE: unavailable metrics, zero-support confusion rows, excluded methods, deterministic PNG metadata, and subject-table hash mismatch. REFACTOR: share common identity columns without changing statistical kernels. Runtime harness: N/A — pure projection/rendering. Rollback: revert only R09 additions while preserving R08. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R09`. <!-- sdd-owner: implementation -->

- [x] **T-15-R10 / WU-R10 — Project paired inference and Holm families.** Finding: C-01. Dependencies: T-15-R09. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `src/pada3dacb/evaluation/tables.py`, `tests/test_evaluation_tables.py`. Forecast: 260–400; hard ≤400. RED: add failing exact-column tests for paired differences, McNemar, and Holm with six fixed comparator slots per direction/policy/statistic family; command `python -m pytest -q tests/test_evaluation_tables.py`. GREEN: project existing kernel results with raw/adjusted p-values, null available reasons, optional McNemar note, and exact table hashes. TRIANGULATE: unavailable comparator, zero discordance, tied p-values, both policies, and no all-pairs rows. REFACTOR: keep projection free of inferential computation. Runtime harness: N/A. Rollback: revert only R10 additions while preserving R08/R09. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R10`. <!-- sdd-owner: implementation -->

- [x] **T-15-R11 / WU-R11 — Compose validated tables through metrics, confusion, bootstrap, paired tests, and Holm.** Finding: C-01. Dependencies: T-15-R10. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `src/pada3dacb/evaluation/report.py`, `src/pada3dacb/evaluation/metrics.py`, `tests/test_evaluation_report.py`, `tests/test_evaluation_metrics_reference.py`. Documentation incident entries remain owned by `documentation-and-validation`. Forecast: 320–400; hard ≤400. Contract amendment authorized 2026-07-25: preserve canonical subject uniqueness while allowing explicitly marked internal bootstrap resamples to contain repeated observations; document the failure and correction in the phase report. RED: add failing orchestration tests that require canonical target tables to invoke `compute_metrics`, `compute_confusion`, all aggregate bootstrap metrics, six prototype comparisons, exact McNemar, five paired bootstrap metrics, and six-slot Holm families in fixed direction/policy/method order; command `python -m pytest -q tests/test_evaluation_report.py`. GREEN: compose existing pure kernels with explicit bootstrap seed/replicate policy and no training/real-gate bypass. TRIANGULATE: one method, all seven, excluded comparator slots, two directions, separate sensitivity, unavailable metrics, and injected call-order spies. REFACTOR: isolate deterministic bundle construction from writing. Runtime harness: synthetic in-memory tables. Rollback: revert only R11 report/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R11`. <!-- sdd-owner: implementation -->

- [x] **T-15-R12 / WU-R12 — Build and atomically commit the exact completed synthetic tree.** Finding: C-01/C-03. Dependencies: T-15-R11. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `src/pada3dacb/evaluation/report.py`, `tests/test_evaluation_output_schema.py`. Forecast: 320–400; hard ≤400. RED: add a failing production-pipeline test requiring every planned root/policy/method artifact, optional `artifact_index.json`, manifest-last completion, and no extra/missing path; command `python -m pytest -q tests/test_evaluation_output_schema.py tests/test_evaluation_report.py`. GREEN: combine R08–R11 projections, completion metadata, staged verification, and existing atomic commit. TRIANGULATE: new output, excluded methods/header-complete files, sensitivity, overwrite, injected projection/commit failure, and cleanup/no completed manifest. REFACTOR: keep one exact allowlist and one commit boundary. Runtime harness: temporary synthetic output root. Rollback: revert only R12 report/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R12`. <!-- sdd-owner: implementation -->

- [x] **T-15-R13 / WU-R13 — Establish failing default-production acceptance before dispatcher edits.** Findings: C-01..C-04. Dependencies: T-15-R12. Owner: `codex` (`complete-integration-and-regression-tests`). Exact paths: `tests/phase15_integration_fixtures.py`, `tests/test_evaluation_modes.py`, `tests/test_evaluation_boundaries.py`, `tests/test_evaluation_regressions.py`. Forecast: 300–400; hard ≤400. RED: remove injected-executor sufficiency and add default-production acceptance for exact completed evaluate, read-only reuse, approved mapping/raw-ID controls, globally absent expected subject, Source-Only exact issue/report, complete manifest/index, and all real gates before statistics; command `python -m pytest -q tests/test_evaluation_modes.py tests/test_evaluation_boundaries.py tests/test_evaluation_regressions.py --basetemp=artifacts/pytest-tmp-phase15-remediation-red`. Finish only when the new assertions fail for the known unwired default evaluate/reuse reasons and save exact failures before R14/R15 production edits. GREEN is intentionally delegated to R14/R15; TRIANGULATE: both directions, policies, selected/all methods, mismatch/partial/output failure; REFACTOR: consolidate only synthetic fixtures without weakening reference tests. Runtime harness: controlled failing default CLI scenarios with input/output snapshots. Rollback: revert only R13 fixture/test deltas. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R13`. <!-- sdd-owner: implementation -->

- [x] **T-15-R14 / WU-R14 — Connect default synthetic evaluate and turn the R13 evaluate RED green.** Finding: C-01. Dependencies: T-15-R13 RED recorded. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `scripts/evaluate.py`, `tests/test_evaluation_cli.py`. Forecast: 280–400; hard ≤400. RED: replay and save the R13 default-evaluate failures before production edits; focused command `python -m pytest -q tests/test_evaluation_cli.py tests/test_evaluation_modes.py -k 'synthetic and not reuse'`. GREEN: default dispatch performs configured discovery, identity/provenance validation, external-population aggregation, R11 statistics, R12 projection/commit, and returns success only after exact completion. TRIANGULATE: both directions, selected/all methods, primary/sensitivity, incomplete visibility, existing output, overwrite, projection/commit failure, and R07 real closure. REFACTOR: keep the script orchestration-only and rerun R13 non-reuse cases. Runtime harness: default synthetic CLI with pre/post input hashes and exact output snapshot. Rollback: revert only R14 script/test deltas, restoring fail-closed synthetic dispatch. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R14`. <!-- sdd-owner: implementation -->

- [x] **T-15-R15 / WU-R15 — Resolve default completed reuse and turn the R13 reuse RED green.** Finding: C-01/C-03. Dependencies: T-15-R14 GREEN. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `scripts/evaluate.py`, `src/pada3dacb/evaluation/report.py`, `tests/test_evaluation_cli.py`, `tests/test_evaluation_reuse.py`. Forecast: 280–400; hard ≤400. RED: replay and save R13 reuse failures before production edits; command `python -m pytest -q tests/test_evaluation_reuse.py tests/test_evaluation_cli.py tests/test_evaluation_modes.py -k reuse`. GREEN: resolve output/runs roots from CLI or approved config, independently rediscover/hash selected identity inputs, and route default completed reuse through exact `verify_reuse` without normalization, statistics, recomputation, creation, deletion, or writes. TRIANGULATE: CLI/config roots, every identity mismatch, incomplete tree, valid/mismatched `artifact_index.json`, rejected `evaluation_index.json`, and exact byte/mode/time snapshot success. REFACTOR: share identity-input resolution with evaluate, then rerun the complete R13 command green. Runtime harness: completed synthetic tree snapshot before/after. Rollback: revert only R15 deltas, restoring fail-closed reuse while preserving R14 evaluate. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R15`. <!-- sdd-owner: implementation -->

- [x] **T-15-R16 / WU-R16 — Record focused and full remediation evidence without repository edits.** Findings: C-01..C-05 evidence gate. Dependencies: T-15-R15. Owner: `opencode` (`tables-report-cli-integration` remediation evidence closeout; this does not execute T-15-16). Exact repository paths: `[]`; discovery targets: all changed paths declared by WU-R01..WU-R15 and immutable input/output snapshots. Forecast: 0 authored lines. RED: collect every unit's saved failing evidence and fail if any production edit predates its RED record. GREEN: run `python -m pytest -q tests/test_evaluation_schemas.py tests/test_evaluation_provenance.py tests/test_evaluation_discovery.py tests/test_evaluation_cli.py tests/test_evaluation_report.py tests/test_evaluation_tables.py tests/test_evaluation_output_schema.py tests/test_evaluation_reuse.py tests/test_evaluation_modes.py tests/test_evaluation_boundaries.py tests/test_evaluation_regressions.py --basetemp=artifacts/pytest-tmp-phase15-remediation-final`; then `python -m pytest -q`; record exact exit/result/duration/warnings. TRIANGULATE: run `python -m ruff check .` and `git diff --check`, verify no real output/training/concept/manuscript/Phase16 path and no `evaluation_index.json`. REFACTOR: N/A — evidence only. Runtime harness: included in focused command; exact results go only to Engram topic `sdd/phase-15-predictive-evaluation/remediation/WU-R16`. Rollback: no repository bytes; failed evidence returns to the owning remediation unit. <!-- sdd-owner: implementation -->

- [x] **T-15-R18 / WU-R18 — Remove observed-population fallback and retain exact validation evidence.** Findings: C-01/C-02 from failed WU-R17. Dependencies: T-15-R16. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `scripts/evaluate.py`, `tests/test_evaluation_cli.py`. Forecast: 160–300; hard ≤400. RED: prove missing external role populations cannot be reconstructed from observed exports and exact candidate/batch issues remain available to reporting. GREEN: require every selected method/direction/policy/role population and return structured validated batches/issues without fallback. TRIANGULATE: globally absent subjects, Source-Only membership, both schema families, privacy. Evidence: `sdd/phase-15-predictive-evaluation/remediation/WU-R18`. <!-- sdd-owner: implementation -->

- [x] **T-15-R19 / WU-R19 — Populate exact status, inclusion, provenance, and computational outputs.** Findings: C-01/C-02. Dependencies: T-15-R18. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `scripts/evaluate.py`, `tests/test_evaluation_modes.py`, `tests/test_evaluation_output_schema.py`. Forecast: 260–400; hard ≤400. RED: completed synthetic output must contain non-empty exact status/inclusion rows and available/not-recorded computational evidence derived from validated candidates/batches. GREEN: project structured evidence without persisting raw identifiers. TRIANGULATE: selected/excluded methods, both directions/policies, incomplete candidates remain fail-closed. Evidence: `sdd/phase-15-predictive-evaluation/remediation/WU-R19`. <!-- sdd-owner: implementation -->

- [x] **T-15-R20 / WU-R20 — Bind exact expected manifest and reuse metadata.** Finding: C-03. Dependencies: T-15-R19. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `src/pada3dacb/evaluation/report.py`, `scripts/evaluate.py`, `tests/test_evaluation_reuse.py`. Forecast: 220–360; hard ≤400. RED: tampered bootstrap, gate states, authorization/config/input mirrors, disposition, and scientific library versions must reject. GREEN: pass and compare exact expected manifest metadata; include bootstrap policy and scientific libraries in evaluation identity. TRIANGULATE: valid index, alias rejection, immutable success. Evidence: `sdd/phase-15-predictive-evaluation/remediation/WU-R20`. <!-- sdd-owner: implementation -->

- [x] **T-15-R21 / WU-R21 — Preserve public gate diagnostics and approved root resolution.** Findings: C-01/C-04. Dependencies: T-15-R20. Owner: `opencode` (`tables-report-cli-integration`). Exact paths: `scripts/evaluate.py`, `tests/test_evaluation_cli.py`. Forecast: 120–240; hard ≤400. RED: public CLI must emit all four unresolved real-gate names and reuse must resolve omitted CLI roots only from explicit approved config. GREEN: print sanitized diagnostics to stderr and fail closed on absent/ambiguous roots. TRIANGULATE: no statistics/output before real gate; unapproved roots reject. Evidence: `sdd/phase-15-predictive-evaluation/remediation/WU-R21`. <!-- sdd-owner: implementation -->

- [x] **T-15-R22 / WU-R22 — Reconcile planning, ownership, report, and evidence provenance.** Finding: C-05. Dependencies: T-15-R21. Owner: `opencode` (`documentation-and-validation`). Exact paths: `openspec/changes/phase-15-predictive-evaluation/tasks.md`, `specs/phase_15_predictive_evaluation/tasks.md`, `specs/phase_15_predictive_evaluation/agent_plan.yaml`, `docs/PHASE15_REPORT.md`. Forecast: 80–180; hard ≤400. Reconcile task counts/next action, remove cross-action path claims, execute the exact ownership command, record exact available RED/GREEN evidence and explicitly mark non-reconstructable historical per-unit counts rather than inventing them. Evidence: `sdd/phase-15-predictive-evaluation/remediation/WU-R22`. <!-- sdd-owner: implementation -->

- [x] **T-15-R23 / WU-R23 — Repeat no-edit remediation validation.** Findings: C-01..C-05. Dependencies: T-15-R22. Owner: `opencode` (`documentation-and-validation`). Exact paths: `[]`. Forecast: 0. Repeat focused/full/install/import/Ruff/diff validation and immutable snapshots; record exact outcomes. Evidence: `sdd/phase-15-predictive-evaluation/remediation/WU-R23`. <!-- sdd-owner: implementation -->

- [x] **T-15-R24 / WU-R24 — Preserve mixed valid/excluded candidate artifacts.** Findings: C-01/C-02 from WU-R17 attempt 2. Dependencies: T-15-R23 and explicit maintainer historical-evidence disposition. Owner: `opencode` integration action. Paths: `scripts/evaluate.py`, `tests/test_evaluation_modes.py`. Forecast/actual remained bounded below 400. Mixed evaluate now retains selected excluded methods in status/inclusion/provenance while statistics and method artifacts include only methods valid in every requested scope. Focused 52 passed. <!-- sdd-owner: implementation -->

- [x] **T-15-R25 / WU-R25 — Validate mixed-candidate correction without edits.** Dependencies: T-15-R24. Owner: `opencode` documentation/validation action. Paths: `[]`. Repeat focused/full/install/import/Ruff/diff evidence before fresh WU-R17. <!-- sdd-owner: implementation -->

## Parent Review and Lifecycle Gates

- [ ] **T-15-R17 / WU-R17 — Obtain fresh independent remediation scope/spec approval.** Finding: C-05. Dependencies: T-15-R25 PASS. Owner: `kimi` (`independent-remediation-scope-spec-approval`). Exact path: `specs/phase_15_predictive_evaluation/remediation_review.md` only. Forecast: 80–160 authored lines; hard ≤400. Independently compare C-01..C-05, proposal/spec/design/requirements/acceptance/protocol/schema, default synthetic runtime evidence, closed real gate, exact changed paths, and the 14-action/60-path/zero-duplicate ownership plan. PASS requires all remediation to remain within evaluation/synthetic-test scope, every RED record to predate production edits, all required outcomes to be evidenced, and no real evaluation/training/concept/manuscript/delivery/Phase16 work. Focused command: the ownership validation command in `agent_plan.yaml` plus read-only inspection of WU-R16 evidence. Runtime harness: N/A — independent post-apply scope/spec review. Rollback: delete only `remediation_review.md`; BLOCK returns to the owning unit. Evidence topic: `sdd/phase-15-predictive-evaluation/remediation/WU-R17`. <!-- sdd-owner: parent -->

- [ ] **T-15-15 — Perform independent final audit.** Dependencies: T-15-R17 PASS; T-15-14 remains historical evidence. Owner: `kimi` (`final-audit`). Owned path: `specs/phase_15_predictive_evaluation/final_audit.md` only. Verify PE-001..015 and AC-15-001..048 traceability, exact agent-plan ownership with zero duplicate paths, no training/experiment-producing modifications, no real results/claims, no Phase 16 work, statistical-review approval, independent reference evidence, and administrative separation of issue #1793. Focused command: `python -m pytest -q tests/test_evaluation_regressions.py tests/test_evaluation_boundaries.py`. Runtime harness: N/A — independent audit consumes existing evidence and does not run real evaluation. Rollback: delete only `final_audit.md`; findings return to their owning implementation task without broadening scope. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-15`. <!-- sdd-owner: parent -->

- [ ] **T-15-16 — Rerun final validation and attest exact evidence without repository edits.** Dependencies: T-15-15 PASS. Owner: `opencode` (`final-validation`). Owned paths: `[]` exactly. Rerun in order and attest command, exit code, exact result, duration, and warnings: `python -m pip install -e .`; `python -c "import pada3dacb; print(pada3dacb.__version__)"`; `python -m pytest -q`; `python -m ruff check .`; `git diff --check`; then the minimum synthetic Phase 15 command `python -m pytest -q tests/test_evaluation_schemas.py tests/test_evaluation_discovery.py tests/test_evaluation_provenance.py tests/test_evaluation_aggregation.py tests/test_evaluation_metrics.py tests/test_evaluation_confusion_matrices.py tests/test_evaluation_bootstrap.py tests/test_evaluation_paired_statistics.py tests/test_evaluation_multiple_testing.py tests/test_evaluation_metrics_reference.py tests/test_evaluation_statistics_reference.py tests/test_evaluation_statistics_edge_cases.py tests/test_evaluation_tables.py tests/test_evaluation_report.py tests/test_evaluation_output_schema.py tests/test_evaluation_reuse.py tests/test_evaluation_cli.py tests/test_evaluation_integration.py tests/test_evaluation_modes.py tests/test_evaluation_boundaries.py tests/test_evaluation_regressions.py --basetemp=artifacts/pytest-tmp-phase15-final-synthetic`. Runtime-harness evidence: include dry-run and validate-only cases, output absence, input pre/post hashes, and proof no training entry point ran; compare against T-15-14 documentation evidence without editing those docs. Persist exact final evidence to Engram topic `sdd/phase-15-predictive-evaluation/final-validation` and return it to the user. Rollback: no repository bytes are owned; a failure returns to the owning implementation task. Issue #1793 still blocks archive/commit/push/PR/release/publication after PASS. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-16`. <!-- sdd-owner: parent -->

- [ ] **T-15-17 — Resolve delivery authorization without changing scientific scope.** Dependencies: T-15-16 PASS and maintainer-selected chain strategy. Owner: parent orchestrator. Owned paths: none. Validate native receipt issue #1793 through the native lifecycle before archive, commit, push, PR, release, or publication; do not create a new scientific dependency, review verdict, or implementation task. Focused command: the exact native `next_transition` supplied by receipt status at delivery time; do not infer it during planning. Runtime harness: N/A — administrative lifecycle gate. Rollback: no repository bytes are owned; stop delivery if authority remains blocked. Completion topic: `sdd/phase-15-predictive-evaluation/tasks/T-15-17`. <!-- sdd-owner: parent -->
